# F01–F10 status at draft source `8cd20ab`

This table maps the independent audit findings to the current draft implementation. “Covered” means the reproduced boundary has a source change and a passing targeted runtime or transaction test; it is not a production rollout claim. The frozen DEV70 and stateful Luna reports still make the overall quality gate **RED**.

| Finding | Current status | Evidence and remaining boundary |
| --- | --- | --- |
| F01 — keyword before intent/state | Covered for reproduced pre-purchase negation, cart correction and payment-selection paths | Realtime intent and sales-cycle tests; no-cart/checkout journey. The wider ownership migration is partial. |
| F02 — checkout value unbound to evidence | Covered for the reproduced forged-field path | Private raw-inbound parser binds field value to the current customer span; unrelated model value cannot reach preview. |
| F03 — PII redaction blocks checkout extraction | Covered for labeled and natural current-turn checkout | Private capture precedes redacted model context; runtime checkout reaches preview. The repository transcript deliberately masks synthetic names/phones/addresses. |
| F04 — split strategy ownership | Partial | C3 is selected on eligible adaptive turns after canonical input and commerce evaluation. Some capability branches and fallback replies still use other owners; the fit-concern runtime fallback is generic. |
| F05 — commerce capability inaccessible | Partial | Current-cart size/color edit, reprice/preview invalidation, explicit alternative search and two-product price binding are implemented. Multi-product attribute/presentation comparison and catalog index coverage remain open. |
| F06 — decision memory | Partial | Existing profile preferences and bounded session budget/occasion/rejected-product context survive a long history window. Unresolved-question memory and broader correction/referent cases remain open. |
| F07 — accepted-send history loss | Covered for the reproduced failure | Accepted Outbox rows reconcile canonical/Redis bot history idempotently without resending; isolated PostgreSQL rollback/recovery fault injection and throwing-port runtime test passed. A real Redis outage was not exercised. |
| F08 — factual wording too rigid | Partial | Responder has adaptive prose with fact-bound realization, but bound facts are still largely fixed sentences. DEV70 has 12 guard rejects and quality/voice gaps; Q081 is a comparison guard false negative. |
| F09 — conversation-to-purchase evaluation gap | Partial | Nine stateful Luna journeys and checkout retest record actual replies, state, fake effects and model stages; two reach internal purchase confirmation. The fit fallback, unjudged DEV70, holdout and POS receipt boundary remain open. |
| F10 — empty MCP allowlist | Covered | Empty/missing allowlist fails closed; focused MCP authentication tests passed. No live configuration or traffic was changed. |

Current §6d gaps: multi-product product-attribute/presentation representation, a verified comparison realization, variant-level stock/fit for specific requests, attachment receipt before media wording, and capability-specific fallback. The ETA source confusion in the original audit has been resolved in code by separating preparation from destination-bound delivery; missing transit still yields no ETA promise. The [full DEV70 review](c3-luna-dev70-8cd20ab-report.md) and [stateful runtime review](c3-luna-stateful-runtime-baff6a5-report.md) give the observed customer-facing limits.
