# Trạng thái Task 2.0A1/2.0A2 r27.1 — 2026-07-29

## Kết quả

Production đang chạy release `20260729-ad-acquisition-r27.1`, commit
`1e9c20e24ce092738afa6732427c12eaf69a203f`, image
`lana-chatbot-app:ad-acquisition-r27.1`.

Task 2.0A1 và 2.0A2 đã được triển khai ở chế độ `SHADOW` cho 100% page test
`1198992073286645`. Đây là analytics sidecar: không thay đổi prompt, nội dung trả
lời, checkout, handoff, offer, quyền gửi hoặc ownership.

## Phạm vi đã triển khai

- Normalize Meta Ads referral và lưu `referral_type`.
- Tạo acquisition session idempotent theo attribution window 168 giờ.
- Nối entry message → reply plan → outbox sequence 0 → Meta accepted hoặc terminal.
- Phân loại re-engagement, meaningful reply, qualified, buying committed,
  purchase confirmed, negated/retracted và barrier/playbook.
- Giữ `max_stage_reached` monotonic, tách khỏi `current_disposition`.
- Chiếu `NO_RESPONSE_1H/24H` từ timestamp; không tạo timer/outbound.
- Dashboard Admin riêng `Phễu khách từ Meta Ads` cùng breakdown theo ad, post,
  product, label, barrier, playbook, version, ngày và touch.
- Admin chỉ đọc `admin_acquisition_sessions_v`; không lộ customer hash, raw message
  hoặc raw event metadata.

## Giới hạn có chủ đích

- `AD_ACQUISITION_WAVE2_INPUT_ENABLED=false`; ad title/context không vào prompt.
- Delivered/read giữ nullable vì chưa có exact Meta receipt adapter.
- Funnel dừng ở `PURCHASE_CONFIRMED`. Không ghi `CONVERTED` khi chưa có
  acknowledgement `ORDER_CREATED` từ POS.
- Raw `photo_url` không được persist trong acquisition session.
- Chỉ referral Ads đã xác nhận tạo session; nguồn khác không được làm đẹp funnel.

## Bằng chứng

- PR `#56` và follow-up Compose `#58`.
- Annotated tag `20260729-ad-acquisition-r27.1`; verified Git bundle SHA-256
  `49cb400ca86fe4d4924c567e668d5d33c80e9a6711565a4a25dc744ad14f3b95`.
- Local và Docker `pnpm check` PASS; 1.032/1.032 test. Contracts 85/85,
  Database 97/97, Worker 299/299, Admin API 56/56.
- Backup production SHA-256
  `8d7e1f252847680e0906775d84b0394ce8b58cfef257ca0bdedf7908bdf57b11`.
- Restore-test PostgreSQL 17 không network: restore dữ liệu thật, migration
  `0024 up → down → up`, grant Admin và PII boundary đều PASS.
- Migration production mới nhất `0024_ad_acquisition_analytics`.
- API, Realtime, Delivery, Admin API và Admin Web healthy, restart 0, log lỗi mới 0.
- API/Admin API/Admin Web nội bộ 200; public Admin 302 sang Authentik.
- Inbox active 0, Meta Outbox active 0, duplicate reply-plan/sequence 0.
- Non-target services giữ nguyên container ID.

## Cutover và rollback

Lần cutover đầu dừng trước migration/env/service do artifact guard Delivery tìm
nhầm literal nằm ở package database. Script xác nhận production vẫn r26.2 và năm
service đều healthy. Guard được sửa sang symbol thực tế trong Delivery binary; lần
cutover thứ hai thành công.

Rollback script nằm tại
`/opt/lana-chatbot/shared/release-artifacts/20260729-ad-acquisition-r27.1-rollback.sh`,
SHA-256 `f7c0d980f76628327cb3067e0349119b6c9162d8fa981b2e889964c87e8cc63a`;
syntax check PASS. Env backup là
`/opt/lana-chatbot/shared/.env.infrastructure.backup-20260729-ad-acquisition-r27.1`.
Rollback application giữ schema additive `0024` và không xóa Inbox, Outbox, Redis,
PostgreSQL hoặc Qdrant.

## Evidence còn chờ

Ngay sau deploy chưa có inbound mới nên acquisition session/event/view đều bằng 0.
Trạng thái hiện tại là `PENDING_HUMAN_TRAFFIC`. Có thể test bằng tin thật trên page;
không cần Human Test Mode riêng và không tạo inbound giả.

Task 2.0A3 chưa bắt đầu. Trước 2.0A3 vẫn cần contract acknowledgement
`ORDER_CREATED` từ POS với `orderId`, `salesCycleId`, trạng thái và idempotency key.