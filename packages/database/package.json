{
  "name": "@lana/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @lana/contracts build && pnpm --filter @lana/commerce-kernel build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @lana/contracts build && pnpm --filter @lana/commerce-kernel build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @lana/contracts build && pnpm --filter @lana/commerce-kernel build",
    "test": "vitest run",
    "prelint": "pnpm --filter @lana/contracts build && pnpm --filter @lana/commerce-kernel build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "migrate": "node dist/migrate.js up"
  },
  "dependencies": {
    "@lana/contracts": "workspace:*",
    "@lana/commerce-kernel": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
