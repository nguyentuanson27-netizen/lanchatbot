{
  "name": "lana-chatbot-app",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r build && pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "check": "pnpm check:release-integrity && pnpm typecheck && pnpm test && pnpm build",
    "check:release-integrity": "node deploy/runtime-state/runtime-state.mjs validate --state deploy/runtime-state/example.json && node deploy/runtime-state/test-runtime-state.mjs && node deploy/runtime-state/release-integrity-guard.mjs && node deploy/track-b-0036-preprod-operator.test.mjs && bash deploy/track-b-0036-preprod-operator.behavior.test.sh && bash deploy/gate-e-release-evidence-reader-access.behavior.test.sh && pnpm check:dataset-boundary",
    "check:dataset-boundary": "node deploy/runtime-state/dataset-boundary-guard.mjs --self-test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv": "8.20.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
