{
  "name": "@lana/admin-control-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @lana/contracts build && pnpm --filter @lana/pancake-handoff build && pnpm --filter @lana/database build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @lana/contracts build && pnpm --filter @lana/pancake-handoff build && pnpm --filter @lana/database build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @lana/contracts build && pnpm --filter @lana/pancake-handoff build && pnpm --filter @lana/database build",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@lana/contracts": "workspace:*",
    "@lana/database": "workspace:*",
    "@lana/pancake-handoff": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
